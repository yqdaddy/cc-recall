name: Release

on:
  release:
    types: [created]

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Install dependencies
        run: bun install

      - name: Run tests
        run: bun test

      - name: Build binaries
        run: |
          mkdir -p dist
          bun build --compile --target=bun-darwin-arm64 --outfile=dist/recall-darwin-arm64 ./src/index.ts
          bun build --compile --target=bun-darwin-x64 --outfile=dist/recall-darwin-x64 ./src/index.ts
          bun build --compile --target=bun-linux-x64 --outfile=dist/recall-linux-x64 ./src/index.ts
          bun build --compile --target=bun-linux-arm64 --outfile=dist/recall-linux-arm64 ./src/index.ts
          bun build --compile --target=bun-windows-x64 --outfile=dist/recall-windows-x64.exe ./src/index.ts

      - name: Upload binaries to release
        uses: softprops/action-gh-release@v1
        with:
          files: |
            dist/recall-darwin-arm64
            dist/recall-darwin-x64
            dist/recall-linux-x64
            dist/recall-linux-arm64
            dist/recall-windows-x64.exe
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}